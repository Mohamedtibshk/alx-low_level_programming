#include "main.h"
#include <stdio>

/**
 * _puts - function that prints a string
 * @str: string to be printed
 * Return: void
 */

void _puts(char *str)
{
	int i;
	char str[i];

	for (i = 0 ; str[i] != '\0' ; i++)
		puts(str);
	puts(str); \\specifically adding new line
}
