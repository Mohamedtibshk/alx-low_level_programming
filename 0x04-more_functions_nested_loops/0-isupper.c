#include "main.h"
/**
 * _isupper - function to check upper letter
 * @c: function parameter
 * Return: 1 or 0
 */

int _isupper(int c)
{
	int c;

	if (c >= 'A' && c <= 'Z')
	return (1);
	else
	{
	return (0);
}
}
